diff --git a/source/standard-modules/neural/CMakeLists.txt b/source/standard-modules/neural/CMakeLists.txt
index fcf7ad45..dfd0e478 100644
--- a/source/standard-modules/neural/CMakeLists.txt
+++ b/source/standard-modules/neural/CMakeLists.txt
@@ -9,20 +9,23 @@ set(neural_output_dir "${CMAKE_BINARY_DIR}/$<CONFIG>/${slang_library_dir}/${SLAN
 set(neural_module_file "${neural_output_dir}/${SLANG_NEURAL_MODULE_FILE_NAME}")
 
 # Copy neural shader files to output directory
-set(neural_copied_files)
+set(neural_copy_commands)
 foreach(shader_file ${neural_shader_sources})
     get_filename_component(shader_name ${shader_file} NAME)
     set(dest_file "${neural_output_dir}/${shader_name}")
-    add_custom_command(
-        OUTPUT ${dest_file}
-        COMMAND ${CMAKE_COMMAND} -E make_directory ${neural_output_dir}
-        COMMAND ${CMAKE_COMMAND} -E copy ${shader_file} ${dest_file}
-        DEPENDS ${shader_file}
-        VERBATIM
-    )
-    list(APPEND neural_copied_files ${dest_file})
+    list(APPEND neural_copy_commands COMMAND ${CMAKE_COMMAND} -E copy_if_different ${shader_file} ${dest_file})
 endforeach()
 
+set(neural_copy_stamp "${CMAKE_CURRENT_BINARY_DIR}/neural-copy-shaders.stamp")
+add_custom_command(
+    OUTPUT ${neural_copy_stamp}
+    COMMAND ${CMAKE_COMMAND} -E make_directory ${neural_output_dir}
+    ${neural_copy_commands}
+    COMMAND ${CMAKE_COMMAND} -E touch ${neural_copy_stamp}
+    DEPENDS ${neural_shader_sources}
+    VERBATIM
+)
+
 # Determine which compiler to use for building the module
 # For cross-compilation, use slang-bootstrap from SLANG_GENERATORS_PATH
 # (slang-bootstrap is a standalone tool with no external dependencies)
@@ -51,7 +54,7 @@ add_custom_command(
     OUTPUT ${neural_module_file}
     COMMAND ${CMAKE_COMMAND} -E copy -t $<TARGET_FILE_DIR:${SLANG_COMPILER}> $<TARGET_RUNTIME_DLLS:${SLANG_COMPILER}>
     COMMAND $<TARGET_FILE:${SLANG_COMPILER}> ${NEURAL_MODULE_DEFINES} neural.slang -o ${SLANG_NEURAL_MODULE_FILE_NAME}
-    DEPENDS ${neural_copied_files} ${SLANG_COMPILER_DEPENDENCY}
+    DEPENDS ${neural_copy_stamp} ${SLANG_COMPILER_DEPENDENCY}
     COMMAND_EXPAND_LISTS
     WORKING_DIRECTORY ${neural_output_dir}
     VERBATIM
@@ -73,3 +76,4 @@ install(
     PATTERN "*.slang"
     PATTERN "*.slang-module"
 )
+

